# Copy to .env and fill in (never commit .env)
GOOGLE_API_KEY=

# Optional
# PORT=8080
# GDRIVE_PARALLEL=36
# Max concurrent downloads to Google (prevents connect timeouts when many threads start at once)
# GDRIVE_MAX_SIMULTANEOUS_DOWNLOADS=10
# GDRIVE_CONNECT_TIMEOUT=30
# GDRIVE_READ_TIMEOUT=600
